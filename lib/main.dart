import 'package:flutter/material.dart';
import 'package:keep_notes/ui/Home.dart';

void main() async {
  runApp(
    MaterialApp(
      title: 'Keep Notes',
      home: Home(),
      debugShowCheckedModeBanner: false,
    )
  );
}